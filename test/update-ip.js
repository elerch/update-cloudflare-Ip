'use strict';
var updateIp = require('../update-ip.js').updateIp;
var expect = require('chai').expect;

describe('updateIp', () => {
  it('should update the IP in CloudFlare', () => {
    expect(true).to.be.false;
  });
});

describe('lambda integration', () => {
});
